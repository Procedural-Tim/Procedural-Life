const propTypes = {
  // default not included
  EXTERNAL: "external",
  PROVIDED: "provided",
}

const paramTypes = {
  DEP: "dependencies",
  DATA: "dataSource",
  LITERAL: "literal",
}

module.exports = { propTypes, paramTypes }
